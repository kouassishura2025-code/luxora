abstract final class LuxoraDurations {
  static const Duration instant = Duration(milliseconds: 120);
  static const Duration quick = Duration(milliseconds: 200);
  static const Duration standard = Duration(milliseconds: 320);
  static const Duration relaxed = Duration(milliseconds: 480);
  static const Duration ceremonial = Duration(milliseconds: 720);
  static const Duration breathe = Duration(milliseconds: 1200);
}
