// Les mocks seront ajoutés au fur et à mesure selon les besoins.
// Ce fichier sert de point central pour tous les mocks Mocktail.

/// À utiliser avec `registerFallbackValue` si des types custom
/// sont passés aux mocks.
void registerFallbacks() {
  // Exemple :
  // registerFallbackValue(TestData.vehicle);
}
